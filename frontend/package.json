{
  "name": "fido-bank-simulator-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src/",
    "format": "prettier --write 'src/**/*.{js,html,css}'",
    "format:check": "prettier --check 'src/**/*.{js,html,css}'"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.4.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.8"
  }
}
