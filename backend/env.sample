# Bank
FIDO_STEPUP_ENABLED=true
RP_ID=localhost
RP_NAME=FIDO Bank Simulator
RP_ORIGIN=http://localhost:5173
# Docker: RP_ORIGIN=http://localhost (nginx port 80)

# JWT (for WebAuthn challenge tokens)
JWT_SECRET=you-should-change-me-in-production
JWT_EXPIRY_SECONDS=300

# CORS and session age
CORS_ORIGINS=http://localhost:5173
# Docker: CORS_ORIGINS=http://localhost
SESSION_MAX_AGE_SECONDS=3600
